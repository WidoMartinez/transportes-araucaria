services:
  - type: web
    name: transportes-araucaria-backend
    env: node
    plan: free
    buildCommand: cd backend && npm install && npm run migrate
    startCommand: cd backend && npm run start:db
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
